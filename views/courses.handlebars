<script src="/public/js/courses.js" defer></script>

<main class="container">
  <h1 class="text-center">Courses</h1>
  {{> course-search-form search=search sortBy=sortBy}}
  <hr />
  <div class="container vstack gap-2">
    {{#if search}}
    <h2>Search results for "{{ search }}"</h2>
    {{/if}}
    {{#each courses as |course|}}
    <div class="card">
      <div class="card-body">
        <div class="row">
          <div class="col">
            <h5 class="card-title">{{ course.courseName }}</h5>
            <h6 class="card-subtitle">{{ course.universityName }}</h6>
            <p class="card-text">{{ course.courseCode }}</p>
            <p class="card-text">{{ course.professors }}</p>
            <p class="card-text">Tags: {{ course.tags }}</p>
          </div>
          <div class="col">
            <input
              name="rating"
              class="review-rating rating mb-2"
              data-min="0"
              data-max="5"
              data-step="0.5"
              value="{{ course.averageRating }}"
            />
          </div>
        </div>
        <a
          class="btn btn-primary stretched-link"
          href="/courses/{{ course._id }}"
          >View Course</a
        >
      </div>
    </div>
    {{/each}}
  </div>
</main>
